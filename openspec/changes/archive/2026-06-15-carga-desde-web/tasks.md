## 1. UI Implementation

- [x] 1.1 Add "Carga desde web" button in `gui/main.py` directly after "Seleccionar XMLs…" button.
- [x] 1.2 Implement the URL input dialog window in `gui/main.py` with compatibility label, entry field, and Aceptar/Cancelar buttons.
- [x] 1.3 Bind the "Carga desde web" button command to open the modal dialog and verify focus behavior.

## 2. Behavior & Dialog Control

- [x] 2.1 Implement cancel logic to close the dialog and ensure no action is taken.
- [x] 2.2 Implement accept logic to close the dialog.

## 3. Documentation & Verification

- [x] 3.1 Update `README.md` to document the new "Carga desde web" button.
- [x] 3.2 Update `CLAUDE.md` to document the UI layout modifications and dialog behavior.
- [x] 3.3 Verify manual execution of the dialog inside the Tkinter application interface.
- [x] 3.4 Commit the changes to the local Git repository.

